return {
  -- Go debugging
  {
    "leoluz/nvim-dap-go",
    config = true,
  },

  -- Rust debugging (via rustaceanvim - handled separately but listed for reference)
  -- Note: rustaceanvim manages its own DAP integration for Rust
}
