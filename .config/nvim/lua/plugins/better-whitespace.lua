return {
    -- Better Whitespace
    -- Highlight trailing whitespace and provide commands to clean it up
    -- Automatically shows spaces, tabs, and trailing whitespace
    { "ntpeters/vim-better-whitespace" },
}
